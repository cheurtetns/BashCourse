#!/bin/bash
#
# It is possible to use regex in conditions thanks to the ~= comparison operator.
#
# EXERCISE: Check if the string contain digits
#
# I AM NOT DONE
#
string="Hello, world!"

if [[ ]]; then
	echo "String does not contain any digits"
else
	echo "String contains at least one digit"
fi
